<div class="container">
  <div class="row">
    <div class="col">
      <h1>Ingredients</h1>

        <ul>
          <% @ingredients.each do |ingredient| %>
        <div>
          <li><%= link_to "#{ingredient.name}", cocktail_ingredient_path(cocktail) %></li>
        </div>
        <% end %>
        </ul>
    </div>
  </div>
</div>

<%= link_to "Enter a new ingredient", new_cocktail_ingredient_path(@ingredient), class: 'btn btn-primary' %>

