<div class="container">
  <div class="row">
    <div class="col">
      <h1>Cocktails</h1>

        <ul>
          <% @cocktails.each do |cocktail| %>
        <div>
          <ul><%= link_to "#{cocktail.name}", cocktail_path(cocktail), class: 'ul' %></ul>
        </div>
        <% end %>
        </ul>
        <%= link_to "Enter a new cocktail", new_cocktail_path(@cocktail), class: 'btn' %>

        <%#= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY", width: 400, height: 300, crop: :fill) %>
        <%#= cl_image_tag("IMAGE_WITH_FACE_ID", width: 150, height: 150, crop: :thumb, gravity: :face) %>


    </div>
  </div>
</div>

